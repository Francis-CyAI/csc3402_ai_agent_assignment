/**
 * This file keeps all utility functions of pieces
 */

function possibleDestinations(piece, currentPositionSquareId, gameState) {
    /**
     * This function takes in a piece and the ID of the square where it is, 
     * and return an array of ids of the squares where the piece can be played.
     * 
     * It does this by first checking the type of piece, whether man or king and
     * then generates the array of possible destination squares accordingly
     * after validating with the current game state.
     */

}

function validPossibleDestinations(piece, possibleDestinations, gameState) {
    /**
     * This functions takes in a piece and all its possible destationations,
     * then validates the possible destinations against the game state,
     * and finally returns an array of all valid destinations for the piece.
     */
}